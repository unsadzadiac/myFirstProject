import { Grid } from "@mui/material";
export const Line = () => {
  return (
    <Grid
      item
      sx={{
        border: "1px solid rgba(0, 0, 0, 1)",
        width: "350px",
      }}
    ></Grid>
  );
};
